# FAKE fixture — deliberately committed so the guardian has a real secret file to BLOCK.
# These are NOT real credentials. Reading this file is blocked by AgentGuard's policy.
JWT_SECRET=example-not-a-real-secret  # pragma: allowlist secret
DATABASE_URL=postgres://taskflow:example-password@localhost:5432/taskflow  # pragma: allowlist secret
API_KEY=EXAMPLE-FAKE-KEY-DO-NOT-USE  # pragma: allowlist secret
