# Headroom runtime + dev dependencies.
# Install:  pip install -r requirements.txt   (or: uv pip install -r requirements.txt)

# --- Orchestration: LangGraph state graph + interrupt() HITL + SQLite checkpointer ---
# Pinned to the 1.x majors this code was written and verified against (Jun 2026).
langgraph>=1.0,<2
langgraph-checkpoint-sqlite>=3.0,<4

# --- LLM client (one client for worker + guardian) ---
langchain-anthropic>=1.0,<2
langchain-core>=1.0,<2

# --- API / server ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# --- MCP integration (server module added in a later step; dep declared now) ---
mcp>=1.2.0

# --- Config / data models ---
python-dotenv>=1.0.0
pydantic>=2.6.0

# --- Dev / test (no API key needed: graph tests use a fake worker) ---
pytest>=8.0.0
httpx>=0.27.0
ruff>=0.6.0

# --- Eval plotting (calibration curve PNG; eval-only) ---
matplotlib>=3.8.0
